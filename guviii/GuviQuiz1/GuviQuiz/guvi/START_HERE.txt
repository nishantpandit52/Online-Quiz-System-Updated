# 🚀 START HERE - AI Question Generation Setup

## What's New?

Your quiz application can now **generate questions using AI** (Google Gemini)!

Questions are generated based on:
- **Domain** (Java, Math, Science, or ANY custom topic)
- **Difficulty** (Easy, Medium, Hard)

---

## ⚡ Quick Start (5 Minutes)

### Step 1: Compile Everything
```bash
cd divyanshguvi
javac *.java
```

**Or use the batch file (Windows):**
```bash
compile_and_run.bat
```

### Step 2: Get Your FREE API Key

1. Go to: **https://makersuite.google.com/app/apikey**
2. Sign in with your Google account
3. Click "Create API Key"
4. Copy your key

### Step 3: Run Setup Wizard
```bash
java GeminiSetup
```

When prompted:
- Paste your API key
- Press Enter to test connection
- Configure settings (recommended: 50% AI)
- Optionally generate sample questions

### Step 4: Test It!
```bash
java TestGeminiIntegration
```

Try generating questions for any topic you want!

---

## 📝 That's It!

Your application is now ready to generate unlimited AI questions!

---

## 🎯 What Can You Do Now?

### Generate Questions for ANY Topic
```java
QuestionBank qb = new QuestionBank();
List<Question> questions = qb.getQuestionsForDomain(
    "Machine Learning",  // Any topic!
    "Medium",            // Easy, Medium, or Hard
    10                   // Number of questions
);
```

### Control AI Percentage
Edit `config.properties`:
```properties
ai.questions.percentage=50  # 50% AI, 50% database
```

### Disable AI (Use Database Only)
```properties
use.ai.questions=false
```

---

## 📚 Need Help?

- **Quick Guide**: Read `QUICK_START.md`
- **Full Documentation**: Read `GEMINI_AI_GUIDE.md`
- **Technical Details**: Read `IMPLEMENTATION_SUMMARY.md`
- **Test Program**: Run `java TestGeminiIntegration`

---

## 🎓 Example Usage

```java
// Initialize
QuestionBank questionBank = new QuestionBank();

// Get mixed questions (AI + Database)
List<Question> javaQuestions = 
    questionBank.getQuestionsForDomain("Java", "Medium", 10);

// Generate for custom topic
List<Question> mlQuestions = 
    questionBank.getQuestionsForDomain("Machine Learning", "Hard", 5);

// Use questions
for (Question q : javaQuestions) {
    System.out.println(q.getText());
    System.out.println("Answer: " + q.getCorrectAnswer());
}
```

---

## ⚙️ Configuration Options

In `config.properties`:

```properties
# Your API key (required)
gemini.api.key=YOUR_KEY_HERE

# Enable AI (true/false)
use.ai.questions=true

# AI percentage (0-100)
# 0 = no AI, 50 = half AI, 100 = all AI
ai.questions.percentage=50

# Cache questions (true/false)
cache.ai.questions=true
```

---

## 🔥 Benefits

Before AI:
- ❌ Limited to pre-written questions
- ❌ Can't add new topics easily
- ❌ Manual question creation needed

After AI:
- ✅ Unlimited questions
- ✅ ANY topic instantly
- ✅ Auto-generated fresh content
- ✅ Adaptive difficulty

---

## ❓ Common Questions

**Q: Is it free?**  
A: Yes! Gemini API has a generous free tier.

**Q: Do I need internet?**  
A: Only for generating new questions. Cached questions work offline.

**Q: Can I use my existing questions?**  
A: Yes! AI supplements your database, doesn't replace it.

**Q: What if API fails?**  
A: App automatically uses database questions.

---

## 🎉 You're Ready!

Run your first AI generation:
```bash
java TestGeminiIntegration
```

Then integrate into your quiz app:
```bash
java Login
```

---

## 📞 Need Support?

1. Check error messages in console
2. Run `java TestGeminiIntegration` to diagnose
3. Verify API key at https://makersuite.google.com/app/apikey
4. Read the documentation files

---

**Happy Quiz Building! 🚀**

*Powered by Google Gemini AI*
